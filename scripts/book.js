export default class Book{
    constructor(number, title, authors, identifier, image){
        this.number = number
        this.title = title
        this. authors = authors
        this.identifier = identifier
        this.image = image
    }
}